<UserControl x:Class="The_Movies.Views.EditBookingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Width="350">
        <TextBlock Text="Rediger booking" FontSize="24" FontWeight="Bold" Margin="0,0,0,20" HorizontalAlignment="Center"/>

        <TextBlock Text="{Binding MovieTitle}" FontWeight="Bold" Margin="0,0,0,4"/>
        <TextBlock Text="{Binding ScreeningInfo}" FontSize="12" Foreground="Gray" Margin="0,0,0,20"/>

        <TextBlock Text="Antal billetter"/>
        <TextBox Text="{Binding TicketCount, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>

        <TextBlock Text="Kundenavn (valgfrit)"/>
        <TextBox Text="{Binding CustomerName, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>

        <TextBlock Text="{Binding StatusMessage}" Foreground="Green" HorizontalAlignment="Center" Margin="0,0,0,10" TextWrapping="Wrap"/>

        <Button Content="Gem" Width="150" Height="35" Command="{Binding SaveCommand}" Margin="0,0,0,10"/>
        <Button Content="Tilbage" Width="150" Height="35" Command="{Binding BackCommand}"/>
    </StackPanel>
</UserControl>