<UserControl x:Class="The_Movies.Views.MainMenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
        <TextBlock Text="The Movies" FontSize="32" FontWeight="Bold" Margin="0,0,0,30" HorizontalAlignment="Center"/>
        <Button Content="Registrer Film" Command="{Binding RegisterMovieCommand}" Width="200" Height="40" Margin="0,0,0,10"/>
        <Button Content="Vis Film" Command="{Binding ShowMovieListCommand}" Width="200" Height="40" Margin="0,0,0,10"/>
        <Button Content="Lav Program" Command="{Binding ShowProgramCommand}" Width="200" Height="40" Margin="0,0,0,10"/>
        <Button Content="Vis Program" Command="{Binding ShowProgramOverviewCommand}" Width="200" Height="40" Margin="0,0,0,10"/>
        <Button Content="Book Billetter" Command="{Binding ShowBookingCommand}" Width="200" Height="40" Margin="0,0,0,10"/>
        <Button Content="Vis Bookinger" Command="{Binding ShowBookingListCommand}" Width="200" Height="40"/>
    </StackPanel>
</UserControl>