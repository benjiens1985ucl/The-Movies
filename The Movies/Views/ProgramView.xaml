<UserControl x:Class="The_Movies.Views.ProgramView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="1200">
    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300"/>
            <ColumnDefinition Width="20"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0" VerticalAlignment="Center">
            <TextBlock Text="Lav program" FontSize="24" FontWeight="Bold" Margin="0,0,0,20" HorizontalAlignment="Center"/>

            <TextBlock Text="Biograf"/>
            <ComboBox ItemsSource="{Binding Cinemas}" DisplayMemberPath="Name" SelectedItem="{Binding SelectedCinema}" Margin="0,0,0,10"/>

            <TextBlock Text="Sal"/>
            <ComboBox ItemsSource="{Binding AvailableHalls}" DisplayMemberPath="Number" SelectedItem="{Binding SelectedHall}" Margin="0,0,0,10"/>

            <TextBlock Text="Film"/>
            <ComboBox ItemsSource="{Binding Movies}" DisplayMemberPath="Title" SelectedItem="{Binding SelectedMovie}" Margin="0,0,0,10"/>

            <TextBlock Text="Dato"/>
            <DatePicker SelectedDate="{Binding SelectedDate}"
                        DisplayDateStart="{Binding Today, Mode=OneWay}"
                        Margin="0,0,0,10"/>

            <TextBlock Text="Filmen tilføjes automatisk efter sidste visning den dag (eller kl. 10:00, hvis salen er tom)." FontSize="11" Foreground="Gray" TextWrapping="Wrap" Margin="0,0,0,10"/>

            <CheckBox Content="Premiere" IsChecked="{Binding IsPremiere}" Margin="0,0,0,20"/>

            <TextBlock Text="{Binding StatusMessage}" Foreground="Green" HorizontalAlignment="Center" Margin="0,0,0,10" TextWrapping="Wrap"/>

            <Button Content="Tilføj til spilleliste" Width="180" Height="35" Command="{Binding SaveCommand}" Margin="0,0,0,10"/>
            <Button Content="Tilbage" Width="150" Height="35" Command="{Binding BackCommand}"/>
        </StackPanel>

        <StackPanel Grid.Column="2">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,10">
                <Button Content="◀" Width="30" Command="{Binding PreviousWeekCommand}" Margin="0,0,10,0"/>
                <TextBlock Text="{Binding WeekLabel}" FontSize="16" FontWeight="Bold" VerticalAlignment="Center"/>
                <Button Content="▶" Width="30" Command="{Binding NextWeekCommand}" Margin="10,0,0,0"/>
            </StackPanel>

            <ItemsControl ItemsSource="{Binding WeekDays}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Columns="7"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="LightGray" BorderThickness="0.5" Margin="2" MinHeight="300">
                            <StackPanel>
                                <TextBlock Text="{Binding Date, StringFormat={}{0:ddd dd/MM}}"
                                           FontWeight="Bold"
                                           HorizontalAlignment="Center"
                                           Margin="0,4,0,4"/>
                                <ItemsControl ItemsSource="{Binding Screenings}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Background="#E1F5EE" CornerRadius="4" Margin="4,2" Padding="4">
                                                <StackPanel>
                                                    <TextBlock Text="{Binding DateTime, StringFormat={}{0:HH:mm}}" FontWeight="Bold" FontSize="11"/>
                                                    <TextBlock Text="{Binding Movie.Title}" FontSize="11" TextWrapping="Wrap"/>
                                                </StackPanel>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
    </Grid>
</UserControl>