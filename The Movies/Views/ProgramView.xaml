<UserControl x:Class="The_Movies.Views.ProgramView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Width="350">
        <TextBlock Text="Lav program" FontSize="24" FontWeight="Bold" Margin="0,0,0,20" HorizontalAlignment="Center"/>

        <TextBlock Text="Biograf"/>
        <ComboBox ItemsSource="{Binding Cinemas}" DisplayMemberPath="Name" SelectedItem="{Binding SelectedCinema}" Margin="0,0,0,10"/>

        <TextBlock Text="Sal"/>
        <ComboBox ItemsSource="{Binding AvailableHalls}" DisplayMemberPath="Number" SelectedItem="{Binding SelectedHall}" Margin="0,0,0,10"/>

        <TextBlock Text="Film"/>
        <ComboBox ItemsSource="{Binding Movies}" DisplayMemberPath="Title" SelectedItem="{Binding SelectedMovie}" Margin="0,0,0,10"/>

        <TextBlock Text="Dato"/>
        <DatePicker SelectedDate="{Binding SelectedDateTime}" 
                    Margin="0,0,0,10"/>

        <TextBlock Text="Starttid"/>
        <ComboBox ItemsSource="{Binding AvailableStartTimes}"
                  SelectedItem="{Binding SelectedStartTime}"
                  Margin="0,0,0,10"/>

        <CheckBox Content="Premiere" IsChecked="{Binding IsPremiere}" Margin="0,0,0,20"/>

        <Button Content="Gem" Width="150" Height="35" Command="{Binding SaveCommand}" Margin="0,0,0,10"/>
        <Button Content="Tilbage" Width="150" Height="35" Command="{Binding BackCommand}"/>
    </StackPanel>
</UserControl>