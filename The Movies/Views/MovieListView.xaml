<UserControl x:Class="The_Movies.Views.MovieListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Width="500">
        <TextBlock Text="Film" FontSize="24" FontWeight="Bold" Margin="0,0,0,20" HorizontalAlignment="Center"/>

        <ListView ItemsSource="{Binding Movies}" 
                  SelectedItem="{Binding SelectedMovie}"
                  Height="300" 
                  Margin="0,0,0,20">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Titel" DisplayMemberBinding="{Binding Title}" Width="200"/>
                    <GridViewColumn Header="Varighed" DisplayMemberBinding="{Binding Duration}" Width="100"/>
                    <GridViewColumn Header="Genre" DisplayMemberBinding="{Binding Genre}" Width="150"/>
                </GridView>
            </ListView.View>
        </ListView>
        <StackPanel Orientation="Horizontal"
                    HorizontalAlignment="Center">

            <Button Content="Slet Film"
                Width="150"
                Height="35"
                Margin="0,0,10,0"
                Command="{Binding DeleteMovieCommand}"/>
            <Button Content="Tilbage" 
                Width="150" 
                Height="35" 
                Command="{Binding BackCommand}" 
                HorizontalAlignment="Center"/>

        </StackPanel>
    </StackPanel>
        
</UserControl>