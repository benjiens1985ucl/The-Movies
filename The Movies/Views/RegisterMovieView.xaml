<UserControl x:Class="The_Movies.Views.RegisterMovieView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Width="300">
        <TextBlock Text="Registrer film" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>

        <TextBlock Text="Titel"/>
        <TextBox Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>

        <TextBlock Text="Varighed (minutter)"/>
        <TextBox Text="{Binding Duration, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>

        <TextBlock Text="Genre"/>
        <ComboBox ItemsSource="{Binding GenreValues}" SelectedItem="{Binding Genre}" Margin="0,0,0,20"/>

        <Button Content="Gem" Width="150" Height="35" Command="{Binding SaveCommand}" Margin="0,0,0,10"/>
        <Button Content="Tilbage" Width="150" Height="35" Command="{Binding BackCommand}"/>
    </StackPanel>
</UserControl>