<UserControl x:Class="The_Movies.Views.ProgramOverviewView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="1200">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Programoversigt" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,15"/>

        <WrapPanel Grid.Row="1" HorizontalAlignment="Center" Margin="0,0,0,15">
            <StackPanel Margin="0,0,15,10">
                <TextBlock Text="Biograf"/>
                <ComboBox ItemsSource="{Binding Cinemas}" DisplayMemberPath="Name" SelectedItem="{Binding SelectedCinema}" Width="140"/>
            </StackPanel>

            <StackPanel Margin="0,0,15,10">
                <TextBlock Text="Sal"/>
                <ComboBox ItemsSource="{Binding AvailableHalls}" DisplayMemberPath="Number" SelectedItem="{Binding SelectedHall}" Width="100"/>
            </StackPanel>

            <StackPanel Margin="0,0,15,10">
                <TextBlock Text="Film"/>
                <ComboBox ItemsSource="{Binding MovieFilters}" SelectedItem="{Binding SelectedMovieFilter}" Width="140"/>
            </StackPanel>

            <StackPanel Margin="0,0,15,10">
                <TextBlock Text="Genre"/>
                <ComboBox ItemsSource="{Binding GenreFilters}" SelectedItem="{Binding SelectedGenreFilter}" Width="120"/>
            </StackPanel>

            <StackPanel Margin="0,0,0,10" VerticalAlignment="Bottom">
                <StackPanel Orientation="Horizontal">
                    <Button Content="◀" Width="30" Command="{Binding PreviousWeekCommand}" Margin="0,0,10,0"/>
                    <TextBlock Text="{Binding WeekLabel}" FontWeight="Bold" VerticalAlignment="Center"/>
                    <Button Content="▶" Width="30" Command="{Binding NextWeekCommand}" Margin="10,0,0,0"/>
                </StackPanel>
            </StackPanel>
        </WrapPanel>

        <ItemsControl Grid.Row="2" ItemsSource="{Binding WeekDays}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Columns="7"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="LightGray" BorderThickness="0.5" Margin="2" MinHeight="350">
                        <StackPanel>
                            <TextBlock Text="{Binding Date, StringFormat={}{0:ddd dd/MM}}"
                                       FontWeight="Bold"
                                       HorizontalAlignment="Center"
                                       Margin="0,4,0,4"/>
                            <ItemsControl ItemsSource="{Binding Screenings}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Background="#E1F5EE" CornerRadius="4" Margin="4,2" Padding="4">
                                            <StackPanel>
                                                <TextBlock Text="{Binding DateTime, StringFormat={}{0:HH:mm}}" FontWeight="Bold" FontSize="11"/>
                                                <TextBlock Text="{Binding Movie.Title}" FontSize="11" TextWrapping="Wrap"/>
                                                <TextBlock Text="{Binding CinemaName}" FontSize="9" Foreground="Gray"/>
                                            </StackPanel>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <Button Grid.Row="3"
                Content="Tilbage"
                Width="150"
                Height="35"
                HorizontalAlignment="Center"
                Margin="0,15,0,0"
                Command="{Binding BackCommand}"/>
    </Grid>
</UserControl>